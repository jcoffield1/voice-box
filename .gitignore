node_modules/
out/
dist/
.DS_Store

# Internal design docs — kept locally, not published to GitHub
docs/
*.db
*.db-shm
*.db-wal
coverage/
playwright-report/
test-results/

# Bundled Python environment — generated by `npm run setup:python`
# Committed to the build artifact but not to source control (can be GBs large)
python/runtime/
python/venv/
